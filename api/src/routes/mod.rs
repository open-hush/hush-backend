//! HTTP route handlers.
//!
//! One module per resource. Wire each module's `router()` into
//! `main.rs` as phase 1 brings it online.
//!
//! TODO(phase-1): add `users.rs`, `devices.rs`, `audio.rs`, `cards.rs`,
//! `device_sync.rs`, `device_events.rs`.
