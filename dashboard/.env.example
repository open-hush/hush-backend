# --- API endpoint -------------------------------------------------------
# Base URL the dashboard uses to call the API, at build and dev time.
# NEXT_PUBLIC_* values are inlined into the client bundle — never put
# server secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8080
